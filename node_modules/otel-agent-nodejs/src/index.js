require('dotenv').config();
const { startTracing } = require('./tracer');

startTracing().then(() => {
  console.log("OpenTelemetry tracing initialized.");
}).catch((error) => console.error("Error initializing tracing", error));
