const { expect } = require('chai');
const { startTracing } = require('../src/tracer');

describe('OpenTelemetry Tracing', () => {
  it('should initialize without errors', async () => {
    await startTracing();
    expect(true).to.be.true; // Ajuste conforme necessário
  });
});
